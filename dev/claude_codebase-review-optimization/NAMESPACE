# Generated by roxygen2: do not edit by hand
# Run devtools::document() to regenerate

# =============================================================================
# EXPORTED FUNCTIONS
# =============================================================================

# Bootstrap Synthetic Data Generation
export(generate_bootstrap_synthetic)
export(detect_variable_types)
export(generate_gbsg_bootstrap_general)
export(compare_datasets_general)

# AFT Data Generating Mechanisms
export(generate_aft_dgm)
export(generate_aft_dgm_flex)
export(simulate_from_dgm)
export(summarize_simulation)
export(find_quantile_for_proportion)

# Factor Processing
export(process_factor_variables)
export(process_covariates_corrected)
export(demonstrate_factor_processing)
export(get_reference_levels)

# Bootstrap Summary Helpers
export(summarize_bootstrap_subgroups)
export(summarize_factor_presence)
export(calculate_original_agreement)

# S3 Methods
S3method(print, aft_dgm)
S3method(print, aft_dgm_flex)

# =============================================================================
# IMPORTS
# =============================================================================

# Base R functions
importFrom(stats,
           as.formula,
           coef,
           median,
           model.matrix,
           quantile,
           rbinom,
           rnorm,
           rexp,
           runif,
           scale,
           sd,
           uniroot)

# survival package
importFrom(survival,
           Surv,
           coxph,
           survreg)

# data.table package
importFrom(data.table,
           data.table,
           as.data.table,
           copy,
           rbindlist,
           setnames,
           setcolorder,
           .N)
