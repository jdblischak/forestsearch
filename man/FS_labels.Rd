% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_FSdata_helpers.R,
%   R/subgroup_consistency_helpers.R
\name{FS_labels}
\alias{FS_labels}
\title{Format subgroup labels for ForestSearch}
\usage{
FS_labels(Qsg, confs_labels)

FS_labels(Qsg, confs_labels)
}
\arguments{
\item{Qsg}{Character. Factor code in format "q\if{html}{\out{<index>}}.\if{html}{\out{<action>}}" where
action 0 = NOT (complement), action 1 = IN (member).}

\item{confs_labels}{Character vector. Labels for each factor, indexed by
factor number.}
}
\value{
Character vector of formatted subgroup labels.

Character. Human-readable label wrapped in braces, e.g.,
"\{age <= 50\}" or "!\{age <= 50\}" for complement.
}
\description{
Converts q-indexed subgroup codes to human-readable labels.

Converts q-indexed codes (e.g., "q1.1", "q3.0") to human-readable labels
using the confs_labels mapping.
}
